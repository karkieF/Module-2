# Built-in Functions – Binary Conversion Using Built-in Functions in Python

## 🎯 Aim

To write a Python program to convert the number **16** into its **binary representation** using built-in Python functions.

## 🧠 Algorithm

1. Assign the value `16` to a variable `a`.
2. Use the built-in `bin()` function to convert the number into binary format.
3. Store the result in a variable.
4. Print the binary value.
5. Stop the program.

## 🧾 Program

```
a = 16

b = bin(a)

print("Binary value is:", b)
```

## Output
<img width="224" height="33" alt="image" src="https://github.com/user-attachments/assets/b3a49b34-e4c8-488c-baed-609ed858faaa" />

## Result

Thus, the Python program to convert a decimal number into binary using the built-in `bin()` function was executed successfully.
